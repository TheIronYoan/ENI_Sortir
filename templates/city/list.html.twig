{% extends 'user/base.html.twig' %}

{% block title %}Bienvenue{% endblock %}

{% block bodyuser %}


    <div class="container" style="background-color: #f6f6f6;padding:15px;">
        <br>
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6 text-center">
           <table class="table">
                    <thead class="thead-light">
                    <tr>
                        <th scope="col" colspan="3" class="text-center">Villes</th>
                    </tr>
                    </thead>
                    <tbody>

                    {% for camp in campus  %}
                        <tr class="bg-white">
                            <td class="text-center">{{ camp.id }}</td>
                            <td class="text-center">{{ camp.name }}</td>
                            <td class="text-center"><a href="{{ path('admin_city_edit', { 'id': camp.id }) }}" class="btn btn-light btn-outline-primary">Editer</a></td>
                        </tr>
                    {% endfor %}
                </table>
                <br>
                <a href="{{ path('admin_city_edit') }}" class="btn btn-light btn-outline-primary">Ajouter</a>
            </div>
        </div>
    </div>
{% endblock %}
