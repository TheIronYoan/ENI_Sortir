{% extends 'user/base.html.twig' %}

{% block title %}Bienvenue{% endblock %}

{% block bodyuser %}

    <div class="container" style="background-color: #f6f6f6;padding:15px;">
        <h3 class="text-center">
            Creation Campus
        </h3>
        <br>
        <div class="container text-center">
            <div class="row">
                <div class="col-md-4"></div>
                <div class="col-md-4">
                    {{ form_start(cityForm) }}


                    {{ form_widget(cityForm) }}
                    <br>
                    <button type="submit"  class="btn btn-light btn-outline-primary">Valider</button>
                    <br>
                    {{ form_end(cityForm) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
